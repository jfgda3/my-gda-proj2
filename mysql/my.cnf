# General settings
[mysqld]
# sql_mode = "STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_SHARE"
# max_connections = 150 # Adjust based on your expected load
# bind-address = "0.0.0.0" # Listen on all interfaces (localhost for local development)
# character-set-server = "utf8mb4" # Unicode support
# Data directory (managed by docker volume)
datadir = "/var/lib/mysql"

